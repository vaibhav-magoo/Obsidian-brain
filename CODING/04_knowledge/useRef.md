[[react]] [[Hooks]]

- automatically save an html element from the component 
- ref is a container with special react features
- we add a special property to the html element called ref and its value will be the variable name you are using with useref