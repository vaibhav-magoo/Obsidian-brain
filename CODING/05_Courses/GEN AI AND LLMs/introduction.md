


- discriminative models vs generative models
	   discriminative models only care about the finding the line that best seperates one class from another.
	   generative models understand the underlying distribution of the data and can generate new synthetic examples that look like the training data

- 