

# CSV files
 - the first row gives the headers of the column
 ![[Pasted image 20251223105021.png]]

- import csv in python
- reader to iterate over the file
```python
import csv
with open("authors.csv","r") as file:

    reader = csv.reader(file)

    next(reader)

    for row in reader:

        print(row[1])
```
- dict reader will return a [[dictionary]] 

---

# relational database
CRUD operations - create , read , update, delete
SQL - structured querry langauage
2 types - relational and non relational
query is a request from a database

# basic keywords
 - select - identifies a column
 - from - identifies a table
